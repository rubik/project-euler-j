NB. This is kinda like cheating :)
NB. The code (m&|@^) is a special[0], i.e. it gets optimized automatically.
NB. In this case J can compute the remainder without even computing the whole
NB. power.
NB.
NB. [0]: http://www.jsoftware.com/help/dictionary/special.htm
echo >: m| 28433 * 2 (m&|@^) 7830457 [ m=:1e10
exit''
