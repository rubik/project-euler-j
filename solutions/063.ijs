NB. The table ^/~ 2+i.20x excludes all powers n^1, so I have to add back the
NB. numbers 1-9 (which are 9), but 4, 8, and 9 are already included, as
NB. 4 = 2^2, 8 = 2^3 and 9 = 3^2, so I only add 9 - 3 = 6.
echo 6+ +/ g"0 ~., ^/~ 2+i.20x
exit''
