NB. Make a table, convert every number to string to sort it and back to
NB. integer, then work on the rows ("1) to find out the one with numbers which
NB. use the same digits.
echo 1e5 + I. (*./@:(2&(=/\)))"1 (/:~&.":)"0 (1e5+i.5e4)*/(>:i.6)
exit''
